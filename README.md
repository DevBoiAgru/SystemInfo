# SystemInfo

A simple utility to easily get information 
about your Linux system. Optionally serves the information over
HTTP as json.

Start with --web or -w command line argument (Make sure it's the 
first argument) to start the HTTP JSON API.